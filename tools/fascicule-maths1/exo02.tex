\section{Exercices d'application}

\rubrique{Degré, égalité et opérations sur les polynômes}

\exo{}\diff{1}
Soit $P(x) = 3x^4 - 2x^3 + 5x^2 - x + 7$ et $Q(x) = x^4 + 3x^3 - 2x^2 + 4x - 1$.
\begin{enumerate}[label=\alph*)]
    \item Déterminer le degré de $P$ et de $Q$.
    \item Calculer $P(x) + Q(x)$ et $P(x) - Q(x)$.
    \item Calculer $P(x) \times Q(x)$ et donner le degré du produit.
\end{enumerate}

\exo{}\diff{1}
Déterminer les réels $a$, $b$ et $c$ tels que les polynômes suivants soient égaux :
\[
P(x) = (a+2)x^3 + (b-1)x^2 + (c+3)x + 5 \quad \text{et} \quad Q(x) = 3x^3 + 4x^2 - 2x + 5.
\]

\exo{}\diff{2}
Soit $P(x) = 2x^3 - 5x^2 + 3x - 1$ et $Q(x) = x^2 - 2x + 1$.
\begin{enumerate}[label=\alph*)]
    \item Effectuer la division euclidienne de $P$ par $Q$.
    \item En déduire l'écriture $P(x) = Q(x) \times D(x) + R(x)$ où $\deg R < \deg Q$.
\end{enumerate}

\rubrique{Racines et factorisation}

\exo{}\diff{1}
Vérifier que $1$ est une racine du polynôme $P(x) = x^3 - 3x^2 + 3x - 1$, puis factoriser $P(x)$.

\exo{}\diff{2}
Soit $P(x) = 2x^3 - 5x^2 - 4x + 3$.
\begin{enumerate}[label=\alph*)]
    \item Montrer que $3$ est une racine de $P$.
    \item Déterminer les réels $a$, $b$, $c$ tels que $P(x) = (x-3)(ax^2 + bx + c)$.
    \item Factoriser complètement $P(x)$.
\end{enumerate}

\exo{}\diff{2}
Soit $P(x) = x^4 - 5x^2 + 4$.
\begin{enumerate}[label=\alph*)]
    \item En posant $X = x^2$, résoudre l'équation $P(x) = 0$.
    \item Factoriser $P(x)$ dans $\R[x]$.
\end{enumerate}

\rubrique{Division suivant les puissances croissantes}

\exo{}\diff{3}
Effectuer la division suivant les puissances croissantes de $P(x) = x^3 + 2x^2 - x + 1$ par $Q(x) = x^2 + x + 1$ à l'ordre $2$.

\rubrique{Fractions rationnelles}

\exo{}\diff{1}
Simplifier la fraction rationnelle $F(x) = \dfrac{x^2 - 4}{x^2 - 3x + 2}$ en précisant son domaine de définition.

\exo{}\diff{2}
Décomposer en éléments simples dans $\R(x)$ la fraction rationnelle :
\[
F(x) = \frac{2x^2 + 3x - 1}{(x-1)(x+2)}.
\]

\exo{}\diff{2}
Soit $F(x) = \dfrac{x^3 - 2x^2 + x - 3}{x^2 - x - 2}$.
\begin{enumerate}[label=\alph*)]
    \item Déterminer le domaine de définition de $F$.
    \item Effectuer la division euclidienne du numérateur par le dénominateur.
    \item En déduire une écriture de $F$ sous la forme $F(x) = ax + b + \dfrac{c}{x-2} + \dfrac{d}{x+1}$.
\end{enumerate}

\rubrique{Signe d'un polynôme et d'une fraction rationnelle}

\exo{}\diff{1}
Étudier le signe du polynôme $P(x) = (x-1)(x+3)$ sur $\R$.

\exo{}\diff{2}
Résoudre dans $\R$ l'inéquation $\dfrac{x^2 - 4}{x^2 - 2x - 3} \geq 0$.

\exo{}\diff{3}
Soit $P(x) = 2x^3 - 3x^2 - 11x + 6$.
\begin{enumerate}[label=\alph*)]
    \item Vérifier que $2$ est une racine de $P$.
    \item Factoriser $P(x)$.
    \item Étudier le signe de $P(x)$ sur $\R$.
\end{enumerate}

\section{Exercices d'entraînement}

\rubrique{Degré, égalité et opérations}

\exo{}\diff{1}
Soit $P(x) = 5x^3 - 2x^2 + 3x - 4$ et $Q(x) = -2x^3 + 4x^2 - x + 1$. Calculer $P+Q$, $P-Q$ et $P \times Q$.

\exo{}\diff{1}
Déterminer $m$ et $n$ réels tels que $P(x) = (m+1)x^2 + (n-2)x + 3$ soit égal à $Q(x) = 2x^2 - 5x + 3$.

\exo{}\diff{2}
Soit $P(x) = x^4 - 3x^3 + 2x^2 - x + 1$ et $Q(x) = x^2 - x + 1$. Effectuer la division euclidienne de $P$ par $Q$.

\exo{}\diff{2}
Déterminer le polynôme $P$ de degré $2$ tel que $P(0) = 1$, $P(1) = 2$ et $P(-1) = 4$.

\rubrique{Racines et factorisation}

\exo{}\diff{1}
Vérifier que $-2$ est une racine de $P(x) = x^3 + 2x^2 - x - 2$, puis factoriser $P$.

\exo{}\diff{2}
Soit $P(x) = 3x^3 - 4x^2 - 5x + 2$. Montrer que $2$ est une racine, puis factoriser $P$.

\exo{}\diff{2}
Factoriser $P(x) = x^4 - 13x^2 + 36$ dans $\R[x]$.

\exo{}\diff{3}
Soit $P(x) = x^3 - 6x^2 + 11x - 6$. Trouver trois racines évidentes et factoriser $P$.

\rubrique{Division suivant les puissances croissantes}

\exo{}\diff{3}
Effectuer la division suivant les puissances croissantes de $P(x) = 1 + x + x^2 + x^3$ par $Q(x) = 1 + x$ à l'ordre $2$.

\exo{}\diff{3}
Soit $P(x) = 2 + 3x - x^2 + x^3$ et $Q(x) = 1 + x + x^2$. Effectuer la division suivant les puissances croissantes à l'ordre $2$.

\rubrique{Fractions rationnelles}

\exo{}\diff{1}
Simplifier $F(x) = \dfrac{x^2 + x - 6}{x^2 - 4}$.

\exo{}\diff{2}
Décomposer en éléments simples $F(x) = \dfrac{3x + 5}{(x-1)(x+3)}$.

\exo{}\diff{2}
Soit $F(x) = \dfrac{x^3 + 2x^2 - x - 2}{x^2 - 1}$. Déterminer le domaine, effectuer la division et décomposer.

\exo{}\diff{3}
Décomposer en éléments simples $F(x) = \dfrac{2x^3 - x^2 + 3x - 1}{x^2(x-1)}$.

\rubrique{Signe et inéquations}

\exo{}\diff{1}
Étudier le signe de $P(x) = (2x-1)(x+4)$.

\exo{}\diff{2}
Résoudre $\dfrac{x^2 - 9}{x^2 - 5x + 6} \leq 0$.

\exo{}\diff{2}
Résoudre $(x^2 - 4)(x+1) \geq 0$.

\exo{}\diff{3}
Soit $P(x) = x^3 - 2x^2 - 5x + 6$. Factoriser et étudier son signe.

\section{Problèmes type Probatoire/Bac}

\sujetbac[Polynômes et applications économiques]
Un commerçant de Douala modélise son bénéfice mensuel $B(x)$ (en milliers de francs CFA) par la fonction $B(x) = -x^3 + 6x^2 + 15x - 20$, où $x$ est le nombre de centaines d'articles vendus ($x \geq 0$).
\begin{enumerate}[label=\arabic*)]
    \item Calculer $B(0)$ et $B(2)$. Interpréter.
    \item Montrer que $1$ est une racine de $B(x)$.
    \item Factoriser $B(x)$ sous la forme $(x-1)(ax^2 + bx + c)$.
    \item Étudier le signe de $B(x)$ pour $x \geq 0$.
    \item En déduire la plage de vente pour laquelle le commerçant réalise un bénéfice positif.
\end{enumerate}

\sujetbac[Étude d'une fraction rationnelle]
Soit $f(x) = \dfrac{x^3 - 2x^2 - x + 2}{x^2 - 3x + 2}$.
\begin{enumerate}[label=\arabic*)]
    \item Déterminer le domaine de définition $D_f$ de $f$.
    \item Factoriser le numérateur et le dénominateur.
    \item Simplifier $f(x)$ sur $D_f$.
    \item Résoudre l'équation $f(x) = 0$ et l'inéquation $f(x) \geq 0$.
\end{enumerate}

\sujetbac[Division et décomposition]
Soit $P(x) = x^4 - 2x^3 + 3x^2 - 4x + 2$ et $Q(x) = x^2 - x + 1$.
\begin{enumerate}[label=\arabic*)]
    \item Effectuer la division euclidienne de $P$ par $Q$.
    \item Effectuer la division suivant les puissances croissantes de $P$ par $Q$ à l'ordre $2$.
    \item Soit $F(x) = \dfrac{P(x)}{Q(x)}$. Décomposer $F$ en éléments simples dans $\R(x)$.
\end{enumerate}

\section{Corrigés}

\corrige{de l'exercice 1}
\begin{enumerate}[label=\alph*)]
    \item $\deg P = 4$, $\deg Q = 4$.
    \item $P+Q = 4x^4 + x^3 + 3x^2 + 3x + 6$ ; $P-Q = 2x^4 - 5x^3 + 7x^2 - 5x + 8$.
    \item $P \times Q$ : degré $4+4=8$.
\end{enumerate}

\corrige{de l'exercice 2}
Par identification :
\[
\begin{cases}
a+2 = 3 \\
b-1 = 4 \\
c+3 = -2 \\
5 = 5
\end{cases}
\Rightarrow a = 1,\ b = 5,\ c = -5.
\]

\corrige{de l'exercice 3}
\begin{enumerate}[label=\alph*)]
    \item Division euclidienne : $2x^3 - 5x^2 + 3x - 1$ par $x^2 - 2x + 1$ donne quotient $2x - 1$, reste $ -x$.
    \item $P(x) = (x^2 - 2x + 1)(2x - 1) - x$.
\end{enumerate}

\corrige{de l'exercice 4}
$P(1) = 1 - 3 + 3 - 1 = 0$. Factorisation : $P(x) = (x-1)(x^2 - 2x + 1) = (x-1)^3$.

\corrige{de l'exercice 5}
\begin{enumerate}[label=\alph*)]
    \item $P(3) = 2 \times 27 - 5 \times 9 - 4 \times 3 + 3 = 54 - 45 - 12 + 3 = 0$.
    \item $P(x) = (x-3)(2x^2 + x - 1)$.
    \item $2x^2 + x - 1 = (2x-1)(x+1)$, donc $P(x) = (x-3)(2x-1)(x+1)$.
\end{enumerate}

\corrige{de l'exercice 6}
\begin{enumerate}[label=\alph*)]
    \item $X = x^2$, $X^2 - 5X + 4 = 0$ donne $X = 1$ ou $X = 4$, donc $x = \pm 1$ ou $x = \pm 2$.
    \item $P(x) = (x-1)(x+1)(x-2)(x+2)$.
\end{enumerate}

\corrige{de l'exercice 7}
Division suivant les puissances croissantes de $P(x) = 1 - x + 2x^2 + x^3$ par $Q(x) = 1 + x + x^2$ à l'ordre $2$ :
On ordonne : $P = 1 - x + 2x^2 + x^3$, $Q = 1 + x + x^2$.
On cherche $A(x) = a_0 + a_1 x + a_2 x^2$ et $R(x) = r_0 x^3 + r_1 x^4$ tels que $P = Q \times A + R$.
Par identification, on trouve $a_0 = 1$, $a_1 = -2$, $a_2 = 3$, et $R = -4x^3 - 2x^4$.
Donc $P = (1 + x + x^2)(1 - 2x + 3x^2) - 4x^3 - 2x^4$.

\corrige{de l'exercice 8}
$F(x) = \dfrac{(x-2)(x+2)}{(x-1)(x-2)} = \dfrac{x+2}{x-1}$, domaine $D = \R \setminus \{1, 2\}$.

\corrige{de l'exercice 9}
On cherche $F(x) = \dfrac{A}{x-1} + \dfrac{B}{x+2}$. En réduisant au même dénominateur et en identifiant :
$2x^2 + 3x - 1 = A(x+2) + B(x-1)$. Pour $x=1$ : $2+3-1 = 4 = 3A \Rightarrow A = \frac{4}{3}$.
Pour $x=-2$ : $8 - 6 - 1 = 1 = -3B \Rightarrow B = -\frac{1}{3}$.
Donc $F(x) = \dfrac{4}{3(x-1)} - \dfrac{1}{3(x+2)}$.

\corrige{de l'exercice 10}
\begin{enumerate}[label=\alph*)]
    \item $D = \R \setminus \{-1, 2\}$.
    \item Division : $x^3 - 2x^2 + x - 3$ par $x^2 - x - 2$ donne quotient $x - 1$, reste $2x - 5$.
    \item $F(x) = x - 1 + \dfrac{2x - 5}{(x-2)(x+1)}$. On décompose $\dfrac{2x-5}{(x-2)(x+1)} = \dfrac{A}{x-2} + \dfrac{B}{x+1}$.
    $A = \frac{4-5}{3} = -\frac{1}{3}$, $B = \frac{-2-5}{-3} = \frac{7}{3}$. Donc $F(x) = x - 1 - \frac{1}{3(x-2)} + \frac{7}{3(x+1)}$.
\end{enumerate}

\corrige{de l'exercice 11}
Tableau de signes : $x-1$ s'annule en $1$, $x+3$ en $-3$.
$P(x) > 0$ pour $x \in ]-\infty, -3[ \cup ]1, +\infty[$ ; $P(x) < 0$ pour $x \in ]-3, 1[$ ; $P(x) = 0$ en $-3$ et $1$.

\corrige{de l'exercice 12}
$\dfrac{x^2 - 4}{x^2 - 2x - 3} = \dfrac{(x-2)(x+2)}{(x-3)(x+1)}$. Tableau de signes :
Valeurs critiques : $-2$, $-1$, $2$, $3$.
$F(x) \geq 0$ pour $x \in ]-\infty, -2] \cup ]-1, 2] \cup ]3, +\infty[$.

\corrige{de l'exercice 13}
\begin{enumerate}[label=\alph*)]
    \item $P(2) = 16 - 12 - 22 + 6 = -12 \neq 0$. Vérifions $P(1) = 2 - 3 - 11 + 6 = -6 \neq 0$. Vérifions $P(-2) = -16 - 12 + 22 + 6 = 0$. Donc $-2$ est racine.
    \item $P(x) = (x+2)(2x^2 - 7x + 3) = (x+2)(2x-1)(x-3)$.
    \item Tableau de signes : racines $-2$, $\frac{1}{2}$, $3$.
    $P(x) > 0$ pour $x \in ]-2, \frac{1}{2}[ \cup ]3, +\infty[$ ; $P(x) < 0$ pour $x \in ]-\infty, -2[ \cup ]\frac{1}{2}, 3[$.
\end{enumerate}

\corrige{du problème 1}
\begin{enumerate}[label=\arabic*)]
    \item $B(0) = -20$ (perte de 20 000 F CFA) ; $B(2) = -8 + 24 + 30 - 20 = 26$ (bénéfice de 26 000 F CFA).
    \item $B(1) = -1 + 6 + 15 - 20 = 0$, donc $1$ est racine.
    \item $B(x) = (x-1)(-x^2 + 5x + 20)$.
    \item $-x^2 + 5x + 20 = 0$ donne $x = \frac{-5 \pm \sqrt{25 + 80}}{-2} = \frac{-5 \pm \sqrt{105}}{-2}$. La racine positive est $x_0 = \frac{5 + \sqrt{105}}{2} \approx 7,62$. Tableau de signes pour $x \geq 0$ : $B(x) > 0$ pour $x \in ]1, x_0[$.
    \item Bénéfice positif pour $1 < x < \frac{5 + \sqrt{105}}{2}$, soit entre 100 et environ 762 articles.
\end{enumerate}

\corrige{du problème 2}
\begin{enumerate}[label=\arabic*)]
    \item $D_f = \R \setminus \{1, 2\}$.
    \item Numérateur : $x^3 - 2x^2 - x + 2 = (x-1)(x^2 - x - 2) = (x-1)(x-2)(x+1)$. Dénominateur : $x^2 - 3x + 2 = (x-1)(x-2)$.
    \item $f(x) = \dfrac{(x-1)(x-2)(x+1)}{(x-1)(x-2)} = x+1$, pour $x \neq 1, 2$.
    \item $f(x) = 0 \Rightarrow x = -1$ (dans $D_f$). $f(x) \geq 0 \Rightarrow x+1 \geq 0$ avec $x \neq 1, 2$, donc $x \in [-1, 1[ \cup ]1, 2[ \cup ]2, +\infty[$.
\end{enumerate}

\corrige{du problème 3}
\begin{enumerate}[label=\arabic*)]
    \item Division euclidienne : $x^4 - 2x^3 + 3x^2 - 4x + 2$ par $x^2 - x + 1$ donne quotient $x^2 - x + 1$, reste $-2x + 1$.
    \item Division suivant les puissances croissantes à l'ordre $2$ : $P = 2 - 4x + 3x^2 - 2x^3 + x^4$, $Q = 1 - x + x^2$. On trouve $A(x) = 2 - 2x - x^2$, $R(x) = -x^3 + 4x^4$.
    \item $F(x) = \dfrac{P(x)}{Q(x)} = x^2 - x + 1 + \dfrac{-2x+1}{x^2 - x + 1}$. Le dénominateur $x^2 - x + 1$ n'a pas de racine réelle (discriminant $-3$), donc la décomposition est $F(x) = x^2 - x + 1 + \dfrac{-2x+1}{x^2 - x + 1}$.
\end{enumerate}